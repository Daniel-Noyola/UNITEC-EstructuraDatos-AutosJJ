#include "../AutosJJ.h"

// void Vista::clientes()
// {
// 	IO::imprimirLinea("Funcionalidad de Clientes no implementada", TEXTO_ADVERTENCIA);
// 	IO::imprimirLinea("Presiona ENTER para volver al dashboard...", "", false);
// 	cin.get();
// }

void Vista::ventas()
{
	IO::imprimirLinea("Funcionalidad de Ventas no implementada", TEXTO_ADVERTENCIA);
	IO::imprimirLinea("Presiona ENTER para volver al dashboard...", "", false);
	cin.get();
}

void Vista::compras()
{
	IO::imprimirLinea("Funcionalidad de Compras no implementada", TEXTO_ADVERTENCIA);
	IO::imprimirLinea("Presiona ENTER para volver al dashboard...", "", false);
	cin.get();
}

void Vista::proveedores()
{
	IO::imprimirLinea("Funcionalidad de Proveedores no implementada", TEXTO_ADVERTENCIA);
	IO::imprimirLinea("Presiona ENTER para volver al dashboard...", "", false);
	cin.get();
}

void Vista::empleados()
{
	IO::imprimirLinea("Funcionalidad de Empleados no implementada", TEXTO_ADVERTENCIA);
	IO::imprimirLinea("Presiona ENTER para volver al dashboard...", "", false);
	cin.get();
}

void Vista::inventario()
{
	IO::imprimirLinea("Funcionalidad de Inventario no implementada", TEXTO_ADVERTENCIA);
	IO::imprimirLinea("Presiona ENTER para volver al dashboard...", "", false);
	cin.get();
}
